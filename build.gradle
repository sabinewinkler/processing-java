plugins {
    id 'java'
}

group 'playground.processing'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.processing', name: 'core', version: '3.3.7'
    compile group: 'org.tinylog', name: 'tinylog', version: '1.3.5'
}
